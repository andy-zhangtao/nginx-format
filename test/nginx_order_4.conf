#pid        logs/nginx.pid;
events {
    worker_connections 1024;
    # server {
        #     listen 80;
        #     location / {
        #         root html;
        #         index index.html index.htm;
        #     }
    # }
}
